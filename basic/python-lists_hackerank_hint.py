command = list(input().split())
print(command)
if command[0] == "insert":
    position = int(command[1])
    elemenet = int(command[2])
    # call the list insert
elif command[0] == "sort":
    # call the list sort
elif command[0] == "remove":
    element = int(command[1])
    # call the list remove
